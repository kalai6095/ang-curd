import { NullfilterPipe } from './nullfilter.pipe';

describe('NullfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NullfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
